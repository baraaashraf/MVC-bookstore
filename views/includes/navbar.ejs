<nav class="navbar navbar-expand-md navbar-dark bg-dark">
  <div class="container-fluid center">
    <button
      class="navbar-toggler"
      type="button"
      data-bs-toggle="collapse"
      data-bs-target="#navbarSupportedContent"
      aria-controls="navbarSupportedContent"
      aria-expanded="false"
      aria-label="Toggle navigation"
    >
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item">
          <a class="nav-link  <%= pageTitle === 'Home'? 'active' : ''%>" href="/">Home</a>
        </li>
        <% if(isUser) {%>
        <li class="nav-item ">
          <a class="nav-link <%= pageTitle === 'Cart' ? 'active' : ''%>" href="/cart">Cart</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" <%= pageTitle === 'Orders'? 'active' : ''%> href="/orders">Orders</a>
        </li>
        <% } else {%>
        <li class="nav-item">
          <a class="nav-link <%= pageTitle === 'Login'? 'active' : ''%>" href="/login">Login</a>
        </li>
        <li class="nav-item">
          <a class="nav-link <%= pageTitle === 'Sign up'? 'active' : ''%>" href="/signup">Sign Up</a>
        </li>
        <% }%> <% if(isAdmin) {%>
        <li class="nav-item">
          <a class="nav-link <%= pageTitle === 'Manage Products'? 'active' : ''%>" href="/admin/products">Manage Products</a>
        </li>
        <li class="nav-item">
          <a class="nav-link <%= pageTitle === 'Manage Orders'? 'active' : ''%>" href="/admin/orders">Manage Orders</a>
        </li>
        <% } %>
      </ul>
      <% if(isUser) {%>

      <form action="/logout" method="post">
        <input type="submit" class="btn btn-danger" value="Logout" />
      </form>
      <% } %>
    </div>
  </div>
</nav>
